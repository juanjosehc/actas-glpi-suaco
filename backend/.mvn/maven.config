# Flags default para todo `mvn` ejecutado en backend/. Solo verbosity: -q
# oculta el progreso INFO de Maven pero deja logs de la app y errores.
# No se hornea -DskipTests aqui para no cambiar la semantica de `mvn test`
# para el usuario; el script de verificacion lo pasa explicitamente.
-q